const ctrlWrapper = require("./ctrlWrapper");
const addValidation = require("./addValidation");
const checkContactsId = require("./checkContactsId");

module.exports = {
  ctrlWrapper,
  addValidation,
  checkContactsId,
};
